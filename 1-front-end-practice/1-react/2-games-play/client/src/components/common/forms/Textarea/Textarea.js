const Textarea = ({ id, name, placeholder, value }) => {

    return (

        <textarea
            id={id}
            name={name}
            placeholder={placeholder}
            value={value}
        />

    );

};

export default Textarea;